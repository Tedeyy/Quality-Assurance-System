create database quality_assurance_system;
use quality_assurance_system;

CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    google_id TEXT UNIQUE,
    email TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL, -- encypted
    fname TEXT NOT NULL,
    lname TEXT NOT NULL,
    birthdate DATE,
    gender ENUM('Male', 'Female', 'Others'),
    address TEXT,
    province TEXT,
    city TEXT,
    barangay TEXT,
    contact_number INT,
    office TEXT,
    position TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE divisions (
    division_id INT AUTO_INCREMENT PRIMARY KEY,
    acronym TEXT NOT NULL,
    name TEXT NOT NULL,
    parent_division_id INT DEFAULT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_division_id) REFERENCES divisions(division_id)
);

CREATE TABLE divisions_offices (
    office_id INT AUTO_INCREMENT PRIMARY KEY,
    acronym TEXT NOT NULL,
    name TEXT NOT NULL,
    division_id INT,
    parent_office_id INT DEFAULT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (division_id) REFERENCES divisions(division_id),
    FOREIGN KEY (parent_office_id) REFERENCES divisions_offices(office_id)
);

CREATE TABLE accreditations(
    accreditation_id INT AUTO_INCREMENT PRIMARY KEY,
    acronym TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    deadline DATE,
    status ENUM ('In Progress', 'Completed', 'Inactive'),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE accreditation_requirement(
    requirement_id INT AUTO_INCREMENT PRIMARY KEY,
    category_id INT,
    name TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES accreditation_categories(category_id)
);

CREATE TABLE accreditation_categories(
    category_id INT AUTO_INCREMENT PRIMARY KEY,
    accreditation_id INT,
    parent_category_id INT DEFAULT NULL,
    name TEXT NOT NULL,
    FOREIGN KEY (accreditation_id) REFERENCES accreditations(accreditation_id),
    FOREIGN KEY (parent_category_id) REFERENCES accreditation_categories(category_id),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);